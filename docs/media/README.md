# Media Assets

Place repository media files here.

For the CHP demo, the intended path is:

```text
docs/media/chp-demo.mp4
```

Suggested sources:

- terminal-only recording using `examples/chp_demo_video.sh`
- narrated walkthrough using the script in `CHP_DEMO_VIDEO.md`

This file exists so the `docs/media/` directory is tracked before the video is added.
